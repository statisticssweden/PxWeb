namespace PXWeb
{
    /// <summary>
    /// Describe which HTML heading structure that will be used for tables
    /// </summary>
    public enum MainHeaderForTablesType
    {
        /// <summary>
        /// The table name will be the top (H1) heading
        /// </summary>
        TableName,
        /// <summary>
        /// The statistic area (level in the selection treeview) will be the top (H1) heading
        /// </summary>
        StatisticArea
    }
}